# Food Rush Architecture Diagram

## 🏗️ System Architecture

### High-Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                           │
│  (React Native Screens, Components, Navigation)                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓ ↑
┌─────────────────────────────────────────────────────────────────┐
│                         Event Bus                                │
│  • user-logout                                                   │
│  • session-expired                                               │
│  • auth-state-changed                                            │
│  • token-refreshed                                               │
└─────────────────────────────────────────────────────────────────┘
                              ↓ ↑
┌─────────────────────────────────────────────────────────────────┐
│                         State Layer (Zustand)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Auth Store  │  │  Cart Store  │  │ Notification │          │
│  │              │  │              │  │    Store     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │Address Store │  │Favorites Store│  │ App Store   │          │
│  │              │  │              │  │              │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓ ↑
┌─────────────────────────────────────────────────────────────────┐
│                         Service Layer                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  API Client  │  │Token Manager │  │  Event Bus   │          │
│  │              │  │              │  │              │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓ ↑
┌─────────────────────────────────────────────────────────────────┐
│                         Backend API                              │
│  (REST API, Authentication, Data)                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

### Login Flow

```
User enters credentials
        ↓
LoginScreen calls useLogin hook
        ↓
useLogin calls authApi.login()
        ↓
API Client sends POST /auth/login
        ↓
Backend validates and returns tokens + user
        ↓
useLogin calls setAuthData()
        ↓
AuthStore stores user + tokens
        ↓
AuthStore emits 'auth-state-changed'
        ↓
Navigation updates to CustomerApp/RestaurantApp
```

### Logout Flow

```
User clicks logout
        ↓
Component calls logout()
        ↓
AuthStore.logout() executes
        ↓
1. Clear tokens (TokenManager)
2. Reset auth state
3. Emit 'user-logout' event
        ↓
Event Bus broadcasts to all listeners
        ↓
Each store clears its own data:
  • CartStore → clears cart
  • NotificationStore → clears notifications
  • AddressStore → clears addresses
  • FavoritesStore → clears favorites
        ↓
RootNavigator listens to event
        ↓
Navigation resets to UserTypeSelection
```

### Token Refresh Flow

```
API request returns 401
        ↓
API Client intercepts error
        ↓
Check if refresh token exists
        ↓
POST /auth/refresh-token
        ↓
Success?
  ├─ Yes → Store new tokens
  │        Emit 'token-refreshed'
  │        Retry original request
  │
  └─ No  → Clear tokens
           Emit 'session-expired'
           AuthStore handles logout
```

---

## 📦 Store Architecture

### Auth Store

```
┌─────────────────────────────────────────┐
│           Auth Store                     │
├─────────────────────────────────────────┤
│ State:                                   │
│  • user: UserProfile | null              │
│  • isAuthenticated: boolean              │
│  • isLoading: boolean                    │
│  • error: string | null                  │
│  • restaurants: Restaurant[]             │
│  • defaultRestaurantId: string | null    │
├─────────────────────────────────────────┤
│ Actions:                                 │
│  • setAuthData()                         │
│  • logout()                              │
│  • checkAuthStatus()                     │
├─────────────────────────────────────────┤
│ Events:                                  │
│  Emits: auth-state-changed               │
│  Emits: user-logout                      │
└─────────────────────────────────────────┘
```

### Cart Store

```
┌─────────────────────────────────────────┐
│           Cart Store                     │
├─────────────────────────────────────────┤
│ State:                                   │
│  • items: CartItem[]                     │
│  • restaurantID: string | null           │
│  • restaurantName: string | null         │
│  • lastActivity: number                  │
├─────────────────────────────────────────┤
│ Actions:                                 │
│  • addItemtoCart()                       │
│  • updateItemQuantity()                  │
│  • removeItem()                          │
│  • clearCart()                           │
├─────────────────────────────────────────┤
│ Events:                                  │
│  Listens: user-logout → clearCart()      │
└─────────────────────────────────────────┘
```

### Notification Store

```
┌─────────────────────────────────────────┐
│        Notification Store                │
├─────────────────────────────────────────┤
│ State:                                   │
│  • notifications: Notification[]         │
│  • unreadCount: number                   │
│  • isLoading: boolean                    │
├─────────────────────────────────────────┤
│ Actions:                                 │
│  • fetchNotifications()                  │
│  • markAsRead()                          │
│  • reset()                               │
├─────────────────────────────────────────┤
│ Events:                                  │
│  Listens: user-logout → reset()          │
└─────────────────────────────────────────┘
```

---

## 🎯 Event Flow Diagram

### Event Bus Communication

```
                    ┌─────────────┐
                    │  Event Bus  │
                    └─────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ↓                 ↓                 ↓
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  Auth Store  │  │  Cart Store  │  │Notification  │
│              │  │              │  │   Store      │
│ Emits:       │  │ Listens:     │  │ Listens:     │
│ • logout     │  │ • logout     │  │ • logout     │
│ • auth-state │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
                          ↓
                  ┌──────────────┐
                  │ RootNavigator│
                  │              │
                  │ Listens:     │
                  │ • logout     │
                  └──────────────┘
```

---

## 🔐 Authentication Flow

### Complete Auth Cycle

```
┌─────────────────────────────────────────────────────────────┐
│                    Initial App Load                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│              Check AsyncStorage for tokens                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
                    Has tokens?
                    ↙         ↘
                  Yes          No
                   ↓            ↓
        ┌──────────────┐  ┌──────────────┐
        │ Authenticated│  │ Show Login   │
        │ Navigate to  │  │ Screen       │
        │ Main App     │  └──────────────┘
        └──────────────┘         ↓
                                Login
                                 ↓
                    ┌──────────────────────┐
                    │ Store tokens         │
                    │ Set user in store    │
                    │ Emit auth-state      │
                    └──────────────────────┘
                                 ↓
                    ┌──────────────────────┐
                    │ Navigate to Main App │
                    └──────────────────────┘
                                 ↓
                    ┌──────────────────────┐
                    │ User interacts       │
                    │ Makes API calls      │
                    └──────────────────────┘
                                 ↓
                         Token expires?
                         ↙         ↘
                       Yes          No
                        ↓            ↓
            ┌──────────────┐  ┌──────────────┐
            │ Refresh token│  │ Continue     │
            │ Success?     │  │ normally     │
            └──────────────┘  └──────────────┘
                ↙         ↘
              Yes          No
               ↓            ↓
    ┌──────────────┐  ┌──────────────┐
    │ Continue     │  │ Emit session │
    │ with new     │  │ expired      │
    │ tokens       │  │ Logout user  │
    └──────────────┘  └──────────────┘
```

---

## 🏛️ Layer Responsibilities

### Presentation Layer
```
┌─────────────────────────────────────────┐
│         Screens & Components             │
├─────────────────────────────────────────┤
│ Responsibilities:                        │
│  • Render UI                             │
│  • Handle user input                     │
│  • Call store actions                    │
│  • Subscribe to store state              │
├─────────────────────────────────────────┤
│ Does NOT:                                │
│  • Make API calls directly               │
│  • Manage business logic                 │
│  • Handle token management               │
└─────────────────────────────────────────┘
```

### State Layer
```
┌─────────────────────────────────────────┐
│         Zustand Stores                   │
├─────────────────────────────────────────┤
│ Responsibilities:                        │
│  • Manage application state              │
│  • Provide actions to update state       │
│  • Persist state to AsyncStorage         │
│  • Listen to events                      │
│  • Emit events                           │
├─────────────────────────────────────────┤
│ Does NOT:                                │
│  • Make API calls (uses hooks)           │
│  • Manage other stores                   │
│  • Handle navigation                     │
└─────────────────────────────────────────┘
```

### Service Layer
```
┌─────────────────────────────────────────┐
│         Services & Utilities             │
├─────────────────────────────────────────┤
│ Responsibilities:                        │
│  • Make API calls                        │
│  • Handle token management               │
│  • Transform errors                      │
│  • Emit events                           │
├─────────────────────────────────────────┤
│ Does NOT:                                │
│  • Manage UI state                       │
│  • Handle navigation                     │
│  • Clear stores directly                 │
└─────────────────────────────────────────┘
```

---

## 🔄 Before vs After

### Before: Tight Coupling

```
┌──────────────┐
│  API Client  │
│              │
│ Manages:     │
│ • Stores ❌  │
│ • Logout ❌  │
│ • State ❌   │
└──────────────┘
       ↓
┌──────────────┐
│  Auth Store  │
│              │
│ Manages:     │
│ • Other      │
│   stores ❌  │
│ • Push ❌    │
└──────────────┘
       ↓
┌──────────────┐
│  Cart Store  │
│              │
│ Depends on:  │
│ • Auth ❌    │
└──────────────┘
```

### After: Loose Coupling

```
┌──────────────┐
│  API Client  │
│              │
│ Emits:       │
│ • Events ✅  │
└──────────────┘
       ↓
┌──────────────┐
│  Event Bus   │
└──────────────┘
   ↓    ↓    ↓
┌────┐┌────┐┌────┐
│Auth││Cart││Noti│
│    ││    ││    │
│Self││Self││Self│
│-con││-con││-con│
│tain││tain││tain│
│ed ✅││ed ✅││ed ✅│
└────┘└────┘└────┘
```

---

## 📊 Complexity Comparison

### Before
```
Complexity: ████████████████████ (High)
Coupling:   ████████████████████ (Tight)
Testing:    ████░░░░░░░░░░░░░░░░ (Hard)
Understand: ████████░░░░░░░░░░░░ (Medium)
```

### After
```
Complexity: ████████░░░░░░░░░░░░ (Low)
Coupling:   ████░░░░░░░░░░░░░░░░ (Loose)
Testing:    ████████████████████ (Easy)
Understand: ████████████████████ (Easy)
```

---

## 🎯 Key Takeaways

1. **Event-Driven**: All cross-cutting concerns use events
2. **Loose Coupling**: Stores don't know about each other
3. **Single Responsibility**: Each module has one job
4. **Type Safety**: TypeScript ensures correctness
5. **Testability**: Easy to test in isolation
6. **Maintainability**: Easy to understand and modify

---

**For complete details, see:**
- `ARCHITECTURE_SIMPLIFICATION.md` - Full documentation
- `SIMPLIFICATION_SUMMARY.md` - Quick summary
- `COMPLETE_REFACTOR_SUMMARY.md` - All changes
