export const OrderApi = {
  // create order
  // view order status
  // see all orders - pending, completed
  // view parrticular order
  // see order history
  // see order details
  // track driver is possibel
};
